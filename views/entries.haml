%h2 Recent Entries
%br/
%form{:action => "/entries", :method => "GET"}
  Number of entries to show (Default is 30):
  %input{:name => "numEntries", :type => "number"}/
  %input{:type => "submit", :value => "Show Entries"}/
%br/
%table{:border => "1"}
  %tbody
    %tr 
      %th Full Message
      %th Temperature
      %th Current
      %th Voltage
      %th Sender
      %th Timestamp
      %th Delete
    - @entries.each do |entry|
      %tr 
        %td #{entry.message}
        %td #{entry.temperature}
        %td #{entry.current}
        %td #{entry.voltage}
        %td #{entry.sender}
        %td #{entry.date_time.getlocal}
        %td
          %a{:href => "/delete?id=#{entry.id}"} Delete
%br/
%a{:href => "/manualAdd"} Add entries for testing <br/>
%a{:href => "/"} Back to Home
